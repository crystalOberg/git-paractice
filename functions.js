"use strict";


function sayHello(name) {
    return "Hello, " + name;
}

   let helloMessage = sayHello(name);
    console.log(helloMessage);

    let myName = "Crystal Moon";
    console.log(sayHello(myName));

function isTwo(number) {
    return number === 2;

    console.log(isTwo(random));
}
    function calculateTip(tipPercent, total) {
    let tipAmount = total * tipPercent;
    return tipAmount;
}
    {
        console.log(calculatedTip(tipPercent:.2, total:.20));
        console.log(calcucatedTip(tipPercent:.0.25, total:,25.50))
        console.log(calculatedTip(tipPercent:.0.15, total:.33.42));
    }
    let userTotal = prompt(message: "Hey what was your total at Chili's today?");
    let userTip = parseFloat(prompt(message: "What percentage would you like to tip?")/100;


    alert("you'll add $" + calculateTip(userTip, userTotal) + " to your bill today.");{

        function applyDiscount(itemPrice, itemDiscount) {
        let disPrice = itemPrice - (itemPrice + itemDiscount);
        return disPrice;

    }

    console.log(applyDiscount(itemPrice: 100, itemDiscount:.2));
    console.log(applyDiscount(itemPrice: 45.99, itemDiscount:0.12));

